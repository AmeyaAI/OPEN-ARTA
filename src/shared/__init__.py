"""R74.4 — shared cross-layer utilities.

This package holds small predicates + helpers that need to be used by
both `src.api.routers` and `src.agents` without creating circular
imports. Modules added here MUST be leaf-level (no project imports).
"""
